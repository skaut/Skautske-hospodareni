# Read by Codeception (codeception.yml params).
# DB credentials are also set in docker-compose environment from docker/.env.
DB_HOST=mysql-test
DB_USER=hskauting
DB_PASSWORD=hskauting
DB_NAME=hskauting
DB_TEST=true