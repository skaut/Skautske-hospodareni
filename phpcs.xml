<?xml version="1.0"?>
<ruleset>
    <arg name="basepath" value="."/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="80"/>
    <arg name="cache" value="nette-temp/.phpcs-cache"/>
    <arg name="colors"/>

    <!-- Ignore warnings, show progress of the run and show sniff names -->
    <arg value="nps"/>

    <file>app</file>
    <rule ref="Doctrine">
        <!-- Right now causes problem with several properties used for Doctrine mapping -->
        <exclude name="SlevomatCodingStandard.Classes.UnusedPrivateElements"/>
    </rule>
</ruleset>
