<div class="alert alert-danger">
    <h4 class="alert-heading">Chybějící kategorie!</h4>
    <p>
        Nepodařilo se načíst všechny rozpočtové kategorie.<br />
        {if $isApproved} <i class="fi fi-rr-check-circle" aria-hidden="true"></i> {else} <i class="fi fi-rr-cross-circle" aria-hidden="true"></i> {/if} Je tábor schválen střediskovou radou?<br />
        {if $canActivateAutocomputedBudget} <i class="fi fi-rr-check-circle" aria-hidden="true"></i> {else} <i class="fi fi-rr-cross-circle" aria-hidden="true"></i> {/if} Máte oprávnění aktivovat automatické dopočítávání rozpočtu?<br />
        {if $canUpdateBudget} <i class="fi fi-rr-check-circle" aria-hidden="true"></i> {else} <i class="fi fi-rr-cross-circle" aria-hidden="true"></i> {/if} Máte oprávnění upravovat rozpočtové kategorie tábora?<br /><br />
        <a n:href="activate!" class="btn btn-primary{if !$canActivate} disabled{/if}">Aktivovat dopočítávání</a>
    </p>
</div>
